import { getETA, updateTraffic } from "./Main";

export async function ETA(u , v , info) {
      return await getETA( u , v) ;
}

export async function  updateG(u , info) {
     updateTraffic(i , info) ;
}